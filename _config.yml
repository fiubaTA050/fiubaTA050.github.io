# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.
#
# If you need help with YAML syntax, here are some quick references for you:
# https://learn-the-web.algonquindesign.ca/topics/markdown-yaml-cheat-sheet/#yaml
# https://learnxinyminutes.com/docs/yaml/
#
# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.

title: TA050 Sistemas Distribuidos I
email: eespina@fi.uba.ar
description: >- # this means to ignore newlines until "baseurl:"
  Curso de FIUBA sobre sistemas distribuidos modernos.
baseurl: "" # the subpath of your site, e.g. /blog
url: "https://fiubata050.github.io" # the base hostname & protocol for your site, e.g. http://example.com
#twitter_username: jekyllrb
#github_username:  jekyll

# Build settings
theme: minima
plugins:
  - jekyll-feed
  - jekyll-sitemap
  - jekyll-seo-tag

# Imagen OG por defecto para TODAS las páginas.
# jekyll-seo-tag (v2.8.0) solo lee `page.image` del front matter, NO `site.image`,
# así que la inyectamos en cada página con los "front matter defaults" de Jekyll.
# Cualquier página puede sobreescribirla declarando su propio `image:`.
# Con una imagen presente, jekyll-seo-tag emite `twitter:card: summary_large_image`
# automáticamente. La URL final se vuelve absoluta usando el `url` de arriba.
defaults:
  - scope:
      path: ""
    values:
      image:
        path: /assets/og-image.png
        alt: "TA050 — Sistemas Distribuidos I (FIUBA)"
        width: 1200
        height: 675

# Analytics (GA4) — solo se carga en producción (jekyll.environment == 'production')
google_analytics: G-5RQ7FJZS8Q

# Navigation menu
header_pages:
  - trabajos-practicos.markdown
  - faq.markdown
  - calendario.markdown
  - programa.markdown

# Exclude from processing.
# The following items will not be processed, by default.
# Any item listed under the `exclude:` key here will be automatically added to
# the internal "default list".
#
# Excluded items can be processed by explicitly listing the directories or
# their entries' file path in the `include:` list.
#
exclude:
  - CLAUDE.md
  - README.md
  - _posts/
  - vendor/
  - .bundle/
  - Gemfile
  - Gemfile.lock
  - node_modules/
